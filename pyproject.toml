[project]
name = "moltbook-humanlike"
version = "0.1.0"
description = "Pipeline to detect linguistically atypical (human-like) posts in the Moltbook dataset"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
